# 监理总控工作台 v1.1.0 更新说明

> 发布日期：2026-08-28
> 构建产物：`jianli-workbench-v1.1.0-setup.exe`（Windows 安装包，nsis）
> 提交信息格式：`监理总控工作台v1.1.0`
> 版本标签：`v1.1.0`

---

## 一、版本与打包方式变更
- 版本号由 **v1.0.0** 升级至 **v1.1.0**。
- 打包方式由「便携版（portable）」改为「**Windows 安装包（nsis）**」：安装时可选择安装目录，自动创建桌面与开始菜单快捷方式，支持标准卸载。
- 安装包命名规范：`jianli-workbench-v${version}-setup.exe`（采用英文仓库名，规避中文文件名在上传链路被剥离的问题）。

## 二、新增功能
### 1. 监理月报（GB/T 50319 规范）
- 月报重构为国家标准规范表格：大标题“监 理 月 报（第 期）”、工程概况与本月主要情况合并表（8×7）、主要内容目录、叙述边框框、签字栏（总监签字 / 机构印章 / 报告期）。
- 数据从各业务模块自动汇总（本月 = 按年月筛选，累计 = 全量）。
- 支持导出规范 DOCX 月报（桌面版调用本地 `gen_monthly_report.py`，网页版提供 `input.json` + 生成批处理）。

### 2. AI 助手模块
- 右侧设置面板支持多供应商（OpenAI / DeepSeek / 通义千问 / Moonshot / 智谱 / SiliconFlow / 自定义）、Base URL、模型名、API Key、温度、系统提示词。
- 桌面版通过 `main.js` 的 `ai:chat` IPC 代理转发，避免前端暴露密钥；网页版保留 `fetch` 直接调用兜底。

### 3. 新增台账模块
- 检查验收（检验批 / 隐蔽 / 分项 / 分部 / 单位 / 预验收 / 竣工）、机械设备、特种作业（新增「证书编号」字段）、方案审查。

### 4. 交互
- 新增「新建台账」浮动按钮（空闲上下 bobbing 浮动）。

## 三、交互与视觉优化
- **五类交互动效**（确认 / 删除 / 高亮 / 浮动 / 悬浮）：
  - 确认：删除确认条从右侧平滑滑入；主按钮点击白色光泽 sweep。
  - 删除：两步确认 → 整行 shake 抖动 → 塌陷淡出移除 + toast。
  - 高亮：去处理跳转 → 侧栏对应模块脉冲 + 记录行 flash 居中。
  - 浮动：右下角返回顶部 FAB（滚动 >420px 才淡入上浮）。
  - 悬浮：按钮 / 链接 / 箭头 / 卡片点击水波纹（ripple）与按压反馈。
- **侧栏跳转高亮改为持续常亮态**：去处理跳转后侧栏模块常亮（紫色渐变 + 发光 + “↪ 已跳转”标记），直到用户下次正常导航才解除。
- **悬浮特效补全**：工程情况行悬停微缩进、实时动态项悬停右移并保留状态底色、链接 / 箭头 / 卡片按压回缩反馈。
- **总览卡片悬停圆角渐变边框**（紫蓝渐变，厚度均匀）。
- 全部动效尊重 `prefers-reduced-motion`。

## 四、总览仪表盘
- 重构为 4 个数字指标卡（本月台账总数 / 待处理问题 / 已签发通知单 / AI 本月调用）+ 2 个大型环形进度卡（工程总体进度 / 投资完成率）+ 待处理问题列表（去处理 / 已处理）。
- 修复总览仪表盘空白 Bug：原 `bindReveal` 仅观察新增 reveal 类元素，导致硬编码 reveal 元素 `opacity` 恒为 0 而空白；改为无条件观察所有目标元素。

## 五、导航与文案
- 「监理实施」→「现场监理」，子项顺序：巡视检查、旁站、检查验收、材料进场、机械设备、见证取样、特种作业。
- 「控制与资料」删除「验收记录」，新增「方案审查」。
- 文案统一：「监理联系单」→「工作联系单」、「旁站」→「旁站记录」。

## 六、其他
- 替换软件图标为统一的多尺寸 ICO（16 / 32 / 48 / 64 / 128 / 256），安装包与任务栏显示新图标。
- 三份前端入口（`app.html` / `监理工作台.html` / `工地监理总控工作台.html`）保持一致。
- **作者署名统一为 ZGX**：`package.json` 的 `author` 字段、生成整改/监理通知单/月报等文档所用的默认签发单位（`defaultNoticeCfg` 的 `supervisor_name`）、以及示例种子数据中的发出单位，全部由“北星小学项目监理部”改为“ZGX”。

---

## 附：本次将提交的文件（待用户“同步到 git”指令后执行）
- `app.html`（桌面版最新，含上述全部改动 — 需覆盖 release 仓库当前 18:54 旧版）
- `main.js`（AI 代理 + 月报 DOCX 路由）
- `preload.js`（fsBridge 桥接）
- `package.json`（版本 1.1.0 + nsis 配置）
- `build.js`（nsis 打包编排）
- `更新说明-v1.1.0.md`（本说明）
