# 监理总控工作台 v1.3.0 更新说明

> 发布日期：2026-09-05
> 版本标签：`v1.3.0`
> 说明：本版接入华为云服务器（Node.js + SQLite 后端），实现电脑端数据与远端数据库的双向同步，与 iOS 移动端共用同一套项目台账。

---

## 一、云同步：接入华为服务器

- 顶栏新增「**云同步**」状态与入口（本地存储状态右侧，灰点=未连接 / 蓝点=同步中 / 绿点=已连接 / 红点=同步失败）。
- 点击顶栏云同步区域打开「**云同步设置**」弹窗，配置：
  - **服务器地址**：默认 `http://1.95.72.129:3000`（可改）
  - **用户名 / 密码**：服务器管理员账号（默认 admin）
  - 提供三个动作：**登录并全量同步**（先上传后下载）/ **仅上传** / **仅下载**，并显示上次同步时间。
- 每次同步前自动登录（token 30 天有效期内免重复登录），登录态保存在本地配置中。

## 二、同步范围与模块映射

- 电脑端 16 类台账数据按「本地项目」自动归入服务器对应项目（按项目名称匹配，服务器无同名项目时**自动创建**），数据按 `project_id` 分区存储。
- 模块映射（电脑端 → 服务器）：

| 电脑端 | 服务器模块 |
|---|---|
| 巡视检查 | xunshi |
| 检查验收 | inspect |
| 旁站记录 | pangzhan |
| 见证取样 | sample |
| 材料进场 | material |
| 机械设备 | machine |
| 特种作业 | special |
| 进度对比 | jindu |
| 投资监理 | touzi |
| 监理规划 | guihua |
| 监理日志 / 监理月报 | rizhi / yuebao（按类型拆分） |
| 例会纪要 | lihui |
| 监理通知单 | tongzhi |
| 来往函件 | laixin |
| 方案审查 | fangan |
| 参建单位 | units |

- 上传为**增量 upsert**（按记录 id / 单位角色去重，重复同步不产生重复数据）；下载把服务器上本地没有的记录合并进本地，手机端录入的数据可在电脑端同步查看。

## 三、网络层增强

- 主进程新增 POST 网络代理（`net:post`），渲染进程新增 `httpPost` 调用；`httpGet` 支持携带请求头（云同步鉴权使用）。
- 全部请求经主进程转发，规避 `file://` 页面的跨域限制；超时 30 秒，失败给出明确提示。

---

## 涉及文件

- `app.html`（云同步配置 / 顶栏入口 / 同步引擎 / 模块映射）
- `main.js`（net:post 网络代理、net:get 支持请求头）
- `preload.js`（httpPost、httpGet 请求头透传）
- `package.json`（版本号 1.3.0）

## 使用提示

1. 首次使用在「云同步设置」填写服务器账号密码，点「登录并全量同步」。
2. 服务器端需已部署 `server-v2.js`（多项目版），数据存 `records` 表、按 `project_id` 与 `module` 分区。
3. 电脑端本地数据仍然完整保留，云端同步不影响离线使用。
