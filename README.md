# 监理总控工作台（Windows 桌面版）

面向房建 / 市政项目监理部的纯前端监理业务管理工具，基于 Electron 打包为 **Windows 安装程序**。覆盖巡视检查、旁站、见证取样、材料、验收、进度、投资、通知单、来往函件、监理日志、例会纪要、监理月报的全流程台账与规范 DOCX 文档生成，并内置 AI 助手、区县级天气与华为云服务器双向同步。

> 所有业务数据保存在本机 `data/` 目录，支持本地离线使用（AI / 天气 / 云同步需联网）。每次启动自动备份最近 7 天数据快照。安装包不含示例数据，首次运行为空状态，可通过云同步获取数据。

---

## 功能模块

| 板块 | 说明 |
| --- | --- |
| 总览 (overview) | 项目概况、**区县级三日天气**（昨日/今日/明日，彩色图标）、待处理事项、投资与进度环形图、参建单位管理 |
| 今日 (today) | 全模块待办聚合，一键跳转处理（工作联系单自动排除，无需回复） |
| 现场监理 | 巡视检查、旁站记录（**一键带入当日天气**）、检查验收、材料进场、机械设备、见证取样（含送检知识库、已取样状态、混凝土26天提醒）、特种作业 |
| 控制与资料 | 进度对比、投资控制（预付款单独核算、投资完成率饼图）、监理规划/细则、监理日志、监理月报、例会纪要、监理通知单、工作联系单、来往函件、方案审查 |
| 单位管理 (units) | 五方主体信息，自动带入通知单 / 联系单等模块 |
| AI 助手 (ai) | 接入 DeepSeek / 通义 / 智谱 / OpenAI 等兼容接口：对话、**连接测试**、各表单 **AI 优化**（9 处）、例会纪要 **AI 识别** |
| 云同步 | 接入华为云服务器（Node.js + SQLite），支持全量同步、增量同步、仅上传、仅下载四种模式，与 iOS 移动端共用同一套项目台账 |

## 台账分页

全部 14 个台账页均支持分页（每页 20 条），分页控件包含首页/上一页/页码/下一页/末页/跳转输入框，筛选搜索时自动重置到第 1 页：
- 见证取样、巡视检查、旁站记录、材料进场、机械设备、特种作业
- 投资监理、监理日志、监理通知单、工作联系单、来往函件、会议纪要
- 监理规划与细则、方案审查

## 文档生成（纯前端，无外部依赖）

- **监理通知单 / 工作联系单 / 监理月报**：逐字段对齐官方模板（GB/T 50319 版式，含合并表格、竖排标签、双节文档、签字栏），由内置 [docxgen.js](docxgen.js) 直接生成 OOXML 并打包 DOCX，生成后自动打开。
- **监理用表**：自定义字段表单导出。
- **见证取样 / 材料进场台账**：支持导出台账（TXT）与打印台账（A4 横向规范表格，含工程名称与施工单位表头）。
- 通知单支持嵌入现场照片（自动压缩，导出为"附件：现场照片"页）。
- 网页模式（直接打开 app.html）下导出为浏览器下载。

## AI 能力

- 各业务表单内置「✨ AI 优化」按钮（巡视、旁站、取样、材料、验收、通知单、联系单），按监理规范语境润色文案。
- 例会纪要支持 Word 上传 / 文本粘贴，本地规则识别与「🤖 AI 识别」双通道抽取清单字段。
- API Key 仅保存在本机 `data/`，桌面版请求经主进程代理转发，前端不暴露密钥；设置面板提供「测试连接」。

## 云同步

- 后端部署于华为云服务器（Node.js + Express + SQLite，端口 3000）。
- 支持四种同步模式：
  - **全量同步**：上传 + 下载覆盖
  - **增量同步**：仅同步上次同步后变更的数据
  - **仅上传**：本地数据上传到服务器
  - **仅下载**：服务器数据下载到本地
- 16 类台账双向同步：巡视/检查/旁站/见证/材料/机械/特种/进度/投资/规划/日志/月报/例会/通知/函件/方案/单位。
- 与 iOS 移动端共用同一套项目台账，实现多端数据互通。

## 技术栈

- **桌面壳**：Electron 31（`main.js` 主进程 + `preload.js` contextIsolation 安全桥）
- **前端**：原生 HTML + CSS + JavaScript（单文件 `app.html`，无前端构建步骤）
- **DOCX**：[docxgen.js](docxgen.js)（自实现 ZIP/CRC32，零依赖）
- **图表**：ECharts 5.5（内联，完全离线）；Word 读取：mammoth（内联）
- **天气**：Open-Meteo 免费接口（主进程 `net:get` 代理，规避 file:// 跨域）
- **云同步后端**：Node.js + Express + SQLite（部署于华为云服务器）

## 目录结构

```
监理工作台-桌面版/
├── app.html              # 应用页面（全部 UI 与业务逻辑，内联第三方库）
├── docxgen.js            # 纯前端 DOCX 生成（通知单 / 联系单 / 监理用表 / 月报）
├── main.js               # Electron 主进程：窗口 / 数据读写 / AI 代理 / 天气代理 / 云同步 / 每日备份
├── preload.js            # 预加载脚本（contextIsolation 安全桥，暴露 fsBridge）
├── build.js              # 打包编排（electron-builder + 产物重命名）
├── build/icon.ico        # 应用图标
├── test_docxgen.js       # docxgen.js 回归测试（node test_docxgen.js）
└── 使用说明.txt / 更新说明-*.md
```

## 安装与运行（使用者）

1. 前往本仓库 **Releases** 下载最新版本 `jianli-workbench-v1.3.1-setup.exe`。
2. 双击安装（桌面 / 开始菜单自动创建快捷方式）。
3. 数据默认保存在安装目录的 `data/`，每次启动自动快照到 `data/backup/日期/`（保留 7 天）。
4. 菜单「文件 → 选择数据保存目录…」可更换数据位置；整目录拷贝即可迁移。
5. 首次运行为空状态，可通过顶栏「云同步」登录服务器获取数据，或手动录入。

> 未做数字签名，Windows SmartScreen 可能提示"未知发布者"，点「仍要运行」即可；杀毒软件误报请加白名单。

## 从源码自行打包（开发者）

```bash
npm install
npm run dist          # 产出 dist/jianli-workbench-v1.3.1-setup.exe
npm start             # 本地调试运行
node test_docxgen.js  # DOCX 生成回归测试
```

首次打包会下载 Electron 二进制，国内网络建议设置镜像：

```bash
set ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/
set ELECTRON_BUILDER_BINARIES_MIRROR=https://npmmirror.com/mirrors/electron-builder-binaries/
```

## 数据安全

- 写入为原子操作（临时文件 + 改名），上一版自动留存 `.bak`。
- 启动时自动滚动备份（7 天），误删可从 `data/backup/` 恢复。
- 配置文件 `settings.json` 读取加入内存缓存，保存采用原子写入。
- 云同步数据存储于华为云服务器 SQLite 数据库，支持全量/增量同步。

## 版本历史

| 版本 | 发布日期 | 主要更新 |
| --- | --- | --- |
| [v1.3.1](https://github.com/z1028574223/jianli-workbench/releases/tag/v1.3.1) | 2026-09-06 | 旁站/巡视状态统一、预付款单独核算、投资完成率饼图、见证取样已取样状态、混凝土26天提醒、云同步四种模式、录报告弹窗优化、全部14个台账页分页 |
| [v1.3.0](https://github.com/z1028574223/jianli-workbench/releases/tag/v1.3.0) | 2026-09-05 | 接入华为云服务器云同步、16类台账双向同步、自动登录、网络层代理 |
| [v1.2.1](https://github.com/z1028574223/jianli-workbench/releases/tag/v1.2.1) | 2026-09-01 | 见证取样台账导出/打印、工程名称与施工单位表头、settings.json 缓存与原子写入 |
| [v1.2.0](https://github.com/z1028574223/jianli-workbench/releases/tag/v1.2.0) | 2026-08-30 | DOCX 生成去 Python 化、AI 助手增强、天气组件、通知单现场照片、数据安全原子写 |
| [v1.1.0](https://github.com/z1028574223/jianli-workbench/releases/tag/v1.1.0) | 2026-08-28 | 监理月报规范表格、AI 助手模块、新增检查验收/机械设备/特种作业/方案审查、交互动效、安装包格式变更 |
| [v1.0.0](https://github.com/z1028574223/jianli-workbench/releases/tag/v1.0.0) | 2026-08-23 | 首个正式版本，核心台账模块、规范文档生成、AI 助手、天气组件 |

## iOS 移动端

本项目另有 iOS 原生版本（SwiftUI），独立仓库：[jianli-workbench-ios](https://github.com/z1028574223/jianli-workbench-ios)，与电脑端共用同一套云同步数据库。

## 许可证

[MIT](LICENSE) © 2026 ZGX
