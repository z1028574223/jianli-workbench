# 监理总控工作台 v1.2.0 更新说明

> 发布日期：2026-08-30
> 构建产物：`监理总控工作台-v1.2.0-setup.exe`（Windows 安装包，nsis）

---

## 一、DOCX 生成彻底去 Python 化（核心变更）
- 新增 `docxgen.js`：纯前端生成 DOCX（OOXML + 自实现 ZIP 打包，零第三方依赖），任何电脑可直接使用，不再依赖本机 Python 环境。
- **监理通知单 / 工作联系单 / 监理月报** 逐字段对齐官方模板（E:\项目\北星小学\监理\监理用表\*.docx）：
  - 通知单/联系单为"单格大框"结构，月报为"封面节 + 表格节"双节文档；
  - 页边距统一上下 2cm、左右 1.8cm，表格边框/字体字号/竖排标签/合并结构与模板一致；
  - 联系单新增 DOCX 导出入口（此前只有打印）。
- 网页版导出不再受限：直接下载 DOCX。
- `main.js` 删除 Python/spawn 相关代码，新增 `docx:save` 原子落盘通道（临时文件 + 改名）。
- 附件支持：月报页移除"下载生成配置"遗留按钮。

## 二、数据安全
- 台账 JSON 写入改为原子写（.tmp → rename），并把上一版留作 .bak。
- 每次启动自动快照备份到 `data/backup/日期/`，保留最近 7 天。

## 三、AI 助手增强
- 设置面板新增「🔌 测试连接」：弹窗显示成功/失败详情（模型、接口、耗时、返回）。
- 各业务表单新增「✨ AI 优化」按钮（9 处）：巡视问题描述、旁站施工情况/发现问题/处理情况、见证取样与材料备注、验收记录、通知单内容/要求、联系单正文，按监理规范语境润色。
- 例会纪要新增「🤖 AI 识别」：粘贴会议文本后由 AI 抽取时间/主持人/参会单位/决议等字段，与本地规则识别互为补充。

## 四、天气组件
- 总览页新增区县级三日天气（昨日/今日/明日），数据源 Open-Meteo（免费、桌面版经主进程代理）。
- 彩色 SVG 天气图标（不依赖系统 emoji 字体），标题居中显示。
- 首次点击设置所在区县，结果缓存 30 分钟，点击刷新、右键更换地区。
- 旁站记录表单新增「🌤 带入今日天气」一键填充。

## 五、监理通知单
- 表单新增现场照片入口（最多 9 张、自动压缩），导出 DOCX 时作为"附件：现场照片"页嵌入。

## 六、工作联系单：去除回复状态
- 联系单无需回复闭环：查看弹窗/表单/列表全部移除状态字段与「保存状态」按钮。
- 来往函件台账中联系单自动标记绿色「不需要回复」（查看弹窗同步，不可改状态）。
- 今日待办的"待回函"统计与提醒排除工作联系单。

## 七、列表过滤
- 来往函件、工作联系单、监理通知单、见证取样台账新增胶囊式关键词过滤框（聚焦高亮、一键清空）。

## 八、界面与体验
- 侧栏底部显示当前版本号；帮助→使用说明同步版本与备份说明。
- 删除不可达的旧模块死代码（合同风险、旧验收记录），移除月报"下载生成配置"遗留按钮。
- app.html 移除重复内联的第三方库 script 块，体积 3.0MB → 2.0MB。
- 使用说明.txt 全面重写，匹配当前功能。

---

## 打包
```bash
npm run dist     # 产出 dist/监理总控工作台-v1.2.0-setup.exe
```
