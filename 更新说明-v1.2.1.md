# 监理总控工作台 v1.2.1 更新说明

> 发布日期：2026-09-01
> 构建产物：`jianli-workbench-v1.2.1-setup.exe`（Windows 安装包，nsis）
> 版本标签：`v1.2.1`
> 说明：本版为 v1.2.0 发布后的增量改进，含见证取样台账导出/打印、导出表头、settings.json 缓存与原子写入三处优化。

---

## 一、见证取样与送检台账：新增导出台账 / 打印台账

- 见证取样与送检台账列表新增「**导出台账**」与「**打印台账**」按钮（与材料进场同款交互）。
- **导出台账**：导出为 TXT 文本，字段含 序号 / 分类 / 材料/试件 / 取样部位 / 规格 / 代表批量 / 取样数 / 取样时间 / 送样时间 / 状态 / 报告编号，文件名 `见证取样台账_日期.txt`。
- **打印台账**：按 A4 横向版式生成带边框的规范表格（标题、表头、监理单位盖章栏），自动触发打印。
- 两者均**与关键词过滤联动**：仅导出 / 打印当前过滤条件下的记录；无匹配时提示。

## 二、见证取样台账：导出 / 打印增加工程名称与施工单位表头

- 导出与打印的台账顶部新增「**工程名称**」与「**施工单位**」两行表头。
- 工程名称自动取记录关联项目（单一项目显示项目名，多项目显示"多个项目"，未关联显示"未设置"）；施工单位自动读取单位管理中的施工单位信息。

## 三、settings.json 读取缓存 + 原子写入

- 应用配置 `settings.json` 的读取加入**内存缓存**（首次读盘后复用，减少重复磁盘 IO）。
- 配置保存改为**原子写入**：先写临时文件 `.tmp` 再 `rename` 覆盖，避免写盘中断导致配置损坏；写入前自动确保目录存在。
- 与台账 JSON 的原子写方案保持一致，配置读写更健壮。

---

## 涉及文件

- `app.html`（见证取样导出台账 / 打印台账 / 表头 / 相关样式）
- `main.js`（settings.json 缓存与原子写入）
