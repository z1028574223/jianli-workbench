## v1.3.9 更新说明

**发布日期**: 2026-09-12
**安装包**: 监理总控工作台-v1.3.9-setup.exe（Windows 64 位）

---

### 【修正】总览页「本月台账总数」漏统计

**问题**：该卡片此前只统计 11 个模块，漏掉了**监理日志、进度对比、投资监理、监理规划、监理用表** 5 个模块，导致数字偏小。

**修正**：改为遍历**全部 16 个台账模块**，并为每个模块指定各自的业务日期字段：

| 模块 | 业务日期字段 |
|---|---|
| 见证取样 | **取样日期**（此前误用录入日期，34 条中有 14 条两者不在同一天） |
| 进度对比 | **月份** |
| 例会纪要 | **会议时间**（存放在表单明细中） |
| 其余模块 | 记录日期 |

**修正后**：本月 39 条（此前显示 38），上月 43 条。

---

### 【修正】总览页「待处理问题」卡片与列表对不上

**问题**：卡片显示 **26**，而下方列表只有 **2** 条，两处长期不一致。根因是卡片与列表各用了一套统计公式：

- 卡片把「已取样」「已送检」（等报告，属正常流程）一律算作问题 → 虚增 23 条
- 卡片把「本月每一次例会」都算作问题（例会是会议记录，不是问题）→ 虚增 2 条

**修正**：卡片与列表统一为**同一份计算结果**，口径与手机端一致。待处理问题只包含以下 10 类：

1. 巡视检查 —— 未处理
2. 旁站记录 —— 存在异常描述
3. 检查验收 —— 不合格 / 待验收
4. 材料进场 —— 未验收 / 待验收 / 验收不合格
5. 机械设备 —— 不合格
6. 见证取样 —— 不合格，或「已取样」的混凝土试块满 **26 天**仍未送检
7. 投资监理 —— 审查中
8. 方案审查 —— 不通过（「修改后通过」视为已通过，不计入）
9. 监理通知单 —— 未签发
10. 来往函件 —— 未回复（工作联系单不计入）

**修正后**：卡片数字 = 列表条数，当前为 **1** 条（一份通知单待签发）。

---

### 【修正】「待处理问题」副标题语义

副标题由「**-23 较上月**」改为「**本月新增 N**」。

原写法有两个问题：符号是反的（问题变多却显示负号），且「较上月」对比的是两个不同口径的数字，读起来容易误解。

---

### 【更换】应用图标

全面启用新图标（1024×1024 高清源，14,855 色）：

- 主程序 exe 图标（任务栏 / 桌面快捷方式 / 文件管理器）
- 安装包图标
- 图标包含 16 / 24 / 32 / 48 / 64 / 128 / 256 共 **7 档尺寸**，小尺寸不再模糊

> 此前主程序一直显示 **Electron 默认图标**（一个原子），现已彻底修复。

---

### 【优化】版本号显示

侧边栏版本号由硬编码改为**随打包版本自动更新**，避免出现「界面显示 1.2.1、实际是 1.3.x」的情况。

数据备份提醒也改为统计**全部 16 个模块**（此前只统计 5 个，总数偏小、提醒偏晚）。

---

### 【修改的文件】

- `app.html`（总览统计口径统一、版本号动态化）
- `package.json`（`signAndEditExecutable` 改为 `true`，图标才能写入 exe）
- `build/icon.ico`、`build/icon_1024.png`、`build/icon_512.png`（新图标）

---

### 【升级提示】

覆盖安装即可，无需卸载重装。本地数据自动保留。

安装后若桌面/任务栏仍显示旧图标，属 Windows 图标缓存未刷新，注销或重启一次即可。
